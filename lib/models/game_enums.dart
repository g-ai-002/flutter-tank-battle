enum Direction { up, down, left, right }

enum TankType { player, enemy }

enum TileType { empty, brick, steel, water, base }

enum GameStatus { idle, playing, paused, levelComplete, gameOver, victory }

enum PowerUpType { shield, speed, firepower }
